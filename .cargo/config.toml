# PyO3's interpreter is platform-specific, so PYO3_PYTHON cannot be pinned globally here.
# The Makefile selects vendor/python-runtime when it exists; CI exports the matching Windows,
# Linux or macOS path after running the platform setup script.
